% References :    % Code by By Ibraheem Al-Dhamari 2010clear all;load data.mat;% import data and reserve variables for plottingx = trainSet.X';y = trainSet.Y';sSize = length(trainSet.Y);sBuffer = 1:1:sSize;xt = testSet.X';  % t-for'test'yt = testSet.Y';tSize = length(testSet.Y);tBuffer = 1:1:tSize;% training Session[w, b, errorBuffer] = PerceptronTrn(x, y);% performance test on tesetSet[e, testerrBuffer]=PerceptronTst(xt,yt,w,b);% presenting resultsdisp(['Test_Errors=' num2str(e/tSize) '  Test Data Size= ' num2str(tSize)])hold onplot(sBuffer, errorBuffer,'r:');plot(tBuffer, testerrBuffer, 'g:');% xlim([0,1000]);xlabel('No. of samples');ylabel('average Error');title('Error Convergence(red for trainSet)');drawnow 